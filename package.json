{
  "name": "rabiun-qa",
  "version": "1.0.0",
  "private": true,
  "description": "Daily automated QA suite for rabiun.com — functional checks, Lighthouse performance history, and a static pass/fail dashboard.",
  "scripts": {
    "test": "playwright test",
    "lighthouse": "node scripts/lighthouse-log.mjs",
    "crosscheck": "node scripts/meta-shopify-crosscheck.mjs",
    "sessions:import": "node scripts/shopify-sessions.mjs",
    "dashboard": "node scripts/build-dashboard.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "chrome-launcher": "^1.1.2",
    "lighthouse": "^12.2.1",
    "node-fetch": "^3.3.2"
  }
}
